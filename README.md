# MoneyFlow 💰

모바일 환경에 최적화된 스마트 가계부 웹앱입니다. 수입/지출을 기록하고, 카드 할부 이자를 포함한 정확한 지출 흐름을 파악할 수 있습니다.

## ✨ 주요 기능

- **Google OAuth 로그인** - 간편한 소셜 로그인
- **수입/지출 등록** - 카테고리별 분류, 결제 수단 선택
- **카드 할부 계산** - 구간별 이자율 자동 계산 (4개월↑ 유이자)
- **대시보드** - 월별 자산 현황, 도넛 차트 통계
- **거래 내역** - 일자별 그룹핑, 수정/삭제 기능
- **데이터 관리** - JSON 내보내기, 초기화 기능

## 🛠 기술 스택

- **Frontend**: Next.js 16, React 19, TypeScript
- **Styling**: Tailwind CSS 4
- **Authentication**: NextAuth.js v5 (Google OAuth)
- **Database**: SQLite (Prisma ORM)
- **Testing**: Vitest

## 📦 설치 및 실행

### 1. 의존성 설치

```bash
npm install
```

### 2. 환경변수 설정

`.env.local` 파일을 생성하고 다음 내용을 추가합니다:

```bash
# NextAuth.js
AUTH_SECRET="your-auth-secret-here"

# Google OAuth
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"
```

> **AUTH_SECRET 생성**: `openssl rand -base64 32`  
> **Google OAuth**: [Google Cloud Console](https://console.cloud.google.com/)에서 OAuth 2.0 클라이언트 ID 발급

### 3. 데이터베이스 설정

```bash
npx prisma db push
```

### 4. 개발 서버 실행

```bash
npm run dev
```

[http://localhost:3000](http://localhost:3000)에서 앱을 확인할 수 있습니다.

## 📋 사용 가능한 스크립트

| 명령어          | 설명               |
| --------------- | ------------------ |
| `npm run dev`   | 개발 서버 실행     |
| `npm run build` | 프로덕션 빌드      |
| `npm run start` | 프로덕션 서버 실행 |
| `npm run test`  | 테스트 실행        |
| `npm run lint`  | ESLint 실행        |

## 📐 할부 이자 계산 공식

```
총 할부 수수료 = 할부원금 × (연이율/12) × (할부개월수+1) / 2
```

### 구간별 연이율

- 1~3개월: **무이자**
- 4~5개월: **연 12%**
- 6~9개월: **연 15%**
- 10~12개월: **연 19%**

### 예시

100만원 6개월 할부: `1,000,000 × (0.15/12) × 7 / 2 = 43,749원`

## 📁 프로젝트 구조

```
src/
├── app/              # Next.js App Router 페이지
│   ├── page.tsx      # 메인 대시보드
│   ├── add/          # 거래 등록
│   ├── edit/         # 거래 수정
│   ├── history/      # 거래 내역
│   ├── settings/     # 설정
│   └── login/        # 로그인
├── components/       # React 컴포넌트
├── actions/          # Server Actions
├── hooks/            # Custom Hooks
├── lib/              # 유틸리티 함수
└── types/            # TypeScript 타입
```

## 🧪 테스트

```bash
npm run test
```

32개의 테스트가 포함되어 있습니다:

- 할부 이자 계산 정확도 (21개)
- 입력 검증 및 Edge Case (11개)

## 📄 라이선스

MIT License
